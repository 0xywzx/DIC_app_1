<!DOCTYPE html>
<html>
  <head>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <% flash.each do |key, value| %>
      <%= content_tag(:div, value, class: "#{key}") %>
    <% end %>
    <nav class="navbar navbar-default">
      <div class="container-fluid">
        <div class="top-header">
          <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#gnavi">
              <span class="sr-only">Rate the Lecture</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="/tops/index">Rate the Lecture</a>
          </div>
          <div id="gnavi" class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
              <li>
                <div class="button_to_index">
                  <a href="/blogs/index" class= "button_to_main">新着</a>
                </div>
              </li>
              <li>
              <form class="navbar-form">
                <div class="form-group">
                  <input type="text" class="form-control" placeholder="">
                </div>
                <button type="submit" class="btn btn-info">検索</button>
              </form>
              </li>
              <% if logged_in? %>
              <li class="button_to_profile">
                <%= link_to "Profile", user_path(current_user.id) %>
              </li>
              <li class="button_to_logout">
                <%= link_to "Logout", session_path(current_user.id), method: :delete %>
              </li>
              <% else %>
              <li class="button_to_login">
                <%= link_to "Sign up / Log in", new_user_path %>
              </li>
              <% end %>
            </ul>
          </div>
        </div>
      </div>
    </nav>
  </head>
  <body>
    <%= yield %>
  </body>
  <footer>
          <p>運営会社 OO 株式会社</p>
          <p>© 2018-2019 Copyright OO Inc.</p>
  </footer>
</html>
